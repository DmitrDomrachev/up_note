name: up_note
description: An new Dart Frog application
version: 0.0.1+2
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.0.0
  in_memory_onboarding_data_source:
    path: packages/in_memory_onboarding_data_source
  onboarding_data_source:
    path: packages/i_onboarding_data_source
  shelf_cors_headers: ^0.1.5
  shelf_swagger_ui: ^1.0.0+2

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
